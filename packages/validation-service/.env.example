# Validation Service Configuration

# Server port (default: 3000)
PORT=3000

# Nostr relay URL (default: wss://peek.hol.is)
RELAY_URL=wss://peek.hol.is

# Admin private key for creating NIP-29 invites (nsec format)
# This should be the private key of the relay admin
# ADMIN_NSEC=nsec1...

# Location validation settings
MAX_DISTANCE_METERS=25.0
MAX_ACCURACY_METERS=20.0

# Invite expiry in seconds (default: 300 = 5 minutes)
INVITE_EXPIRY_SECONDS=300