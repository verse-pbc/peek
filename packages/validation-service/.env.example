# Validation Service Configuration
#
# SECURITY WARNING: Copy this file to .env and add your real keys
# Never commit .env files to version control - they are gitignored for security

# Server port (default: 3000)
PORT=3000

# Nostr relay URL (default: wss://communities2.nos.social)
RELAY_URL=wss://communities2.nos.social
PUBLIC_RELAY_URL=wss://communities2.nos.social

# REQUIRED: Relay's secret key for managing groups (hex format)
# This allows the service to create groups and add members directly
# Generate a new key or use your relay's admin key
RELAY_SECRET_KEY=your_relay_secret_key_hex_here

# Admin private key for creating NIP-29 invites (nsec format) - DEPRECATED
# Now using relay secret key for direct group management
# ADMIN_NSEC=nsec1...

# REQUIRED: NIP-59 Gift Wrap Service Keys for location validation via Nostr events
# Generate with: node -e "const {generateSecretKey, getPublicKey, nip19} = require('nostr-tools'); const sk = generateSecretKey(); const pk = getPublicKey(sk); console.log('nsec:', nip19.nsecEncode(sk)); console.log('npub:', nip19.npubEncode(pk));"
# Or use: nak key generate
SERVICE_NSEC=nsec1your_service_private_key_here
SERVICE_NPUB=npub1your_service_public_key_here

# Alternatively, provide SERVICE_SECRET_KEY in hex format (either SERVICE_NSEC or SERVICE_SECRET_KEY required)
# SERVICE_SECRET_KEY=your_service_secret_key_hex_here

# Location validation settings
MAX_DISTANCE_METERS=25.0
MAX_ACCURACY_METERS=20.0

# Invite expiry in seconds (default: 300 = 5 minutes)
INVITE_EXPIRY_SECONDS=300